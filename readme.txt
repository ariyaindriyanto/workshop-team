Install GIT Software (git-scm.com)
Install VSCODE (Software yang direkomendasikan)
Daftar Github.com 
New Repositori
copy URL GIT https://github.com/xxx/xxx.git
Open folder, klik kanan Git Bash Here
Terminal Git
git clone <URL GIT>
cd <nama folder / nama repositori>
git config --global user.name “<username>”
git config --global user.email “<email>”
git add <nama file>
git add .
git rm -f <nama file>
git commit -m “commitan”
git push -u origin master
git pull
git checkout -b <nama file>
git checkout <nama file>